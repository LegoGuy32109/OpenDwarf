[package]
name = "opendwarf-sdk"
version = "0.1.0"
edition = "2024"
description = "Rust SDK for authoring Open Dwarf mods compiled to WebAssembly."
license = "MIT"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
