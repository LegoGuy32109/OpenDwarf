[package]
name = "world_runtime"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = { version = "2.0.1", features = ["serde"] }
js-sys = "0.3.76"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
world_sim = { path = "../world_sim" }

[features]
default = []
