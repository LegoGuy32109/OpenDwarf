[package]
name = "world_tiles"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.18", features = ["bevy_sprite_render"] }
serde = { version = "1.0.219", features = ["derive"] }
world_sim = { path = "../world_sim" }
